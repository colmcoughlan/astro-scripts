Need to patch remotecommand.py with fionn mpi function and include fionn plugin in lofarpipe plugin path
Then can call fionn plugin to set up hosts correctly
Note make_cluster_desc.py needs to be called by the head node to each fionn run to have correct host names.
